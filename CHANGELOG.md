# Changelog

本仓库遵循 [语义化版本](https://semver.org/lang/zh-CN/)：`主版本.次版本.补丁`。

## [1.4.1] - 2026-08-29

### 修复
- FFmpeg 失败错误分类：服务端把 FFmpeg 失败按关键词映射为结构化错误码（ENCODER_MISSING / INVALID_INPUT / PERMISSION_DENIED / DISK_FULL 等），前端状态徽标悬浮显示具体失败原因。
- jobId 长度限制：上限 64 字符，防止批量高并发时产生极长 key。
- stderr 内存优化：按行消费，只保留最后 200 行，替代原截尾 256KB 方案。

### 优化
- SSE 断线重连：EventSource 断开后指数退避重连（3s → 30s 上限），进度条不再卡死。
- 前端失败状态展示：错误信息填充到状态徽标 title 属性，悬浮可见具体原因。

## [1.4.0] - 2026-08-29

### 新增
- 便携版 zip 内文件改为统一放在 `audio-extractor/` 子目录中：解压后只出现一个干净的文件夹，拖到任何位置都能直接用，不再平铺一堆文件。
- `build-portable.bat` 优化：自动排除自身、支持无 Node 时仅警告不中断、中文路径修复为系统编码（UTF-8 BOM），脚本整体更稳健。

### 变更
- 便携版使用方式更新：解压 zip → 进入 `audio-extractor/` 文件夹 → 双击「启动工具-最小化.bat」即可使用。
- 「安装到桌面.bat」仍然保留在 zip 内，有桌面图标需求的用户仍可使用。

## [1.3.3] - 2026-08-27

### 新增
- 新增「安装到桌面.bat」：双击即可在桌面创建带图标的「音频提取工具」快捷方式。

### 优化
- 便携包 README 与使用说明同步更新。

## [1.3.2] - 2026-08-27

### 修复
- **中文文件名乱码**：服务端（multer/busboy）把 multipart 里的 filename 按 Latin-1 解码，UTF-8 中文文件名被误解码成乱码。新增 `fixUtf8Name()` 在两条上传路径统一还原。
- **启动脚本解析错乱**：`start.bat` 换行符为 LF（Unix 风格），cmd.exe 解析时把 `cd /d`、`if errorlevel` 拆成碎片报错；`启动音频提取工具.bat` 编码为 UTF-8 在中文系统控制台下乱码。两者统一重写为 CRLF + 系统编码，Node 检测改为「同目录 node.exe 优先 → PATH 兜底」。

### 优化
- 结果卡片新增格式徽标（MP3 / M4A / WAV…）
- 下载按钮点击后短暂反馈「已开始 ✓」
- 文件列表「已完成」状态展示「大小 → 转换后文件名」
- 移除「失败」状态徽标里的 `ⓘ` 残留字符

## [1.3.1] - 2026-08-27

### 修复
- health 接口版本号硬编码为 1.3.0 → 改为统一从 `package.json` 读取

### 优化
- 头部 logo 去除渐变底框，直显原初版图标（52px）
- 文件大小支持 GB 单位显示
- 结果汇总条文案中性化

## [1.3.0] - 2026-08-27

### 新增
- 桌面级启动体验：最小化静默启动器、页面底部「退出服务」按钮
- 下载名根治：磁盘 hash 名 → 原始文件名

### 优化
- UI 打磨、文档更新

## [1.2.0] - 2026-08-26

### 新增
- 零依赖便携版（自带 Node 运行时 + FFmpeg，解压即用）
- PWA 缓存修复

### 优化
- 便携包改用精简版 FFmpeg（体积 -23%）

## [1.1.0] - 2026-08-25

正式版系列公开发行的起点。

## [0.1.1] - 2026-08-24

公开预览期加固版：稳定性与健壮性修复。

## [0.1.0] - 2026-08-23

公开预览期首发：核心链路验证版。