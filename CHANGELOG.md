# Changelog

本仓库遵循 [语义化版本](https://semver.org/lang/zh-CN/)：`主版本.次版本.补丁`。

## 仓库维护 - 2026-08-30

### v1.6.3 发布说明（默认监听收紧）
- **背景**：v1.6.2 及更早版本默认监听 `0.0.0.0`（开放局域网），工具本身无鉴权，局域网内其他设备可访问，存在安全隐患；本工具定位为纯本地离线工具，不应默认对外开放。
- **改动**：v1.6.3 默认监听改为 `127.0.0.1`（仅本机），不开放局域网；如需局域网共享可手动 `BIND=0.0.0.0 node server.js`（仅限可信网络）。
- **不建议使用旧版本的原因**：v1.6.2 及更早版本默认开放局域网且无鉴权，在公共/不可信网络环境下存在被他人访问的风险；日常使用请升级到 **v1.6.3**。
- 旧版本代码不做修改（仅本说明档提示风险）。

## [1.6.3] - 2026-08-30

### 安全
- **默认监听收紧为仅本机**：`BIND` 默认值由 `0.0.0.0` 改为 `127.0.0.1`，启动后仅本机可访问（`http://127.0.0.1:8912`），不再默认开放局域网；如需局域网共享，可手动设置环境变量 `BIND=0.0.0.0` 开启（工具无鉴权，仅限可信网络）。

## 仓库维护 - 2026-08-30（续）

### Release 清理与恢复（更正记录）
- **初测误判（2026-08-30 上午）**：测试目录位于含点路径（`.openclaw\tmp\...`），v1.3.3 及更早版本因 withExt() 文件名解析缺陷（路径含点时输出文件名被误截）呈现「下载接口 404」，据此下架了 v0.1.0 / v0.1.1 / v1.1.0 / v1.2.0 / v1.3.0 / v1.3.1 / v1.3.2 / v1.3.3 共 8 个 Release。
- **重测更正（2026-08-30 上午）**：在无点目录的正常路径（如 `D:\Tools\audio-extractor`）下逐一重测：v0.1.0 / v0.1.1 / v1.2.0 / v1.3.0 / v1.3.1 / v1.3.2 / v1.3.3 **提取 + 下载全流程全部通过**（下载 HTTP 200）；「下载 404」仅出现在路径含点目录时，v1.5.1 已修复该缺陷。
- **恢复动作**：7 个可用版本已从 git tag 重建便携包（内置 node.exe + FFmpeg，结构与原版一致，抽查实测通过）并重新发布；v1.1.0 从未附带资产，维持下架。
- 保留 v1.5.1 / v1.5.2 / v1.6.0 / v1.6.1 / v1.6.2（实测提取+下载全流程通过）。

### 修复
- **「下载最新版.bat」版本号写死 v1.5.2**：更新为 v1.6.2，并加维护注释（发布新版本时需同步更新）。

## [1.6.2] - 2026-08-29

### 修复
- **分片上传文件名含 Windows 非法字符时合并失败**：上传文件名若包含 `:` `*` `?` `"` `<` `>` `|` 等字符，合并阶段会因文件名非法而失败（500）；现在合并前统一清洗为安全字符，文件名含非法字符也能正常完成上传提取。

### 优化
- `/api/extract-from-path` 响应补 `mode` 字段（与 `/api/extract` 对称，前端 SSE 断线时仍可识别无损直拷/转码）。
- PWA 图标规范化：新增 512×512 图标并补齐 manifest 声明（此前单张 572px 图声明为 192px、缺 512 安装图）；补 `lang` 元数据；HTML 补 ICO 兼容引用与 iOS apple-touch-icon。
- 注释错字修正（兑now→兑现、兑底→兜底）。

## [1.6.1] - 2026-08-29

### 修复
- **高级选项与流拷贝冲突**：用户显式设置比特率 / 采样率 / 声道时，强制走重编码——此前源音轨编码匹配会直接 stream copy，导致 320k 等高音质设置被静默忽略、输出为源音质（影响 M4A / MP3 / FLAC / Opus 四个支持直拷的格式，自 v1.4.1 引入直拷机制起存在）。

### 优化
- 完成列表区分展示「直拷完成」徽章（此前直拷与转码完成均显示「已完成」）。

## [1.6.0] - 2026-08-29

### 新增
- **智能流拷贝扩展到 MP3 / FLAC**：此前仅 M4A / Opus 优先尝试 stream copy，现在 MP3 / FLAC 也纳入——源音轨编码与输出容器匹配时直接复制数据流（不解码不重编码、秒出且 100% 无损），不匹配自动回退重编码（AAC→M4A、MP3→MP3、Opus→Opus、FLAC→FLAC 均命中直拷路径）。
- **界面实时显示提取模式**：SSE 进度与结果接口新增 `mode` 字段（copy=无损直拷 / `transcode`=重编码），前端进度条与文件状态徽章区分展示「无损直拷中」与「转码中 x%」，用户一眼可知本次是秒出还是耗时转码。

### 优化
- 服务端控制台日志补充提取模式：流拷贝完成 / 回退重编码 / 重编码完成均有明确记录。

## [1.5.2] - 2026-08-29

### 修复
- **PWA 离线缓存未生效**：`public/index.html` 缺少 Service Worker 注册代码，`service-worker.js` 从未被加载（缓存/离线能力是死代码）；补上注册逻辑，离线缓存与断网回退真正生效（失败不影响使用，仅失去离线能力）。
- **package-lock.json 版本不同步**：lock 文件 version 停留在 1.1.0，与 package.json 不一致；同步为 1.5.2。

### 新增
- **「下载最新版.bat」一键下载脚本**（仓库根目录 + 随包分发）：双击自动下载最新版便携包到当前目录，国内多镜像自动回退（ghfast.top → gh-proxy.com → GitHub 直连），解决 GitHub 下载慢问题；README 同步补充下载脚本说明与镜像表格。

### 清理
- 删除 `tools/` 下三个过时打包脚本（`build.bat` 写死 v0.1.1、`build.local.example.bat`、`make-portable.ps1` 写死 v1.2.0）：与根目录 `build-portable.bat`（自动读 package.json 版本）功能重复且版本严重落后，易误导；历史保留在 git 中。

## [1.5.1] - 2026-08-29

### 修复（三个真实缺陷）
- **便携包缺 node.exe**：打包脚本 node 路径检测在 `setlocal enabledelayedexpansion` 下失效（`%%NODEEXE%%` 写法），导致 v1.4.0–v1.5.0 的 zip 均未打入 Node 运行时，无 Node 环境的机器无法启动；改用 `%NODEEXE%` 并验证包内 node.exe 就位。
- **输出路径被点目录截断**：`withExt` 用 `inputPath.replace(/\.[^.]+$/, '')` 去扩展名，路径中含带点目录（如 `C:\Users\john.doe\...`、`.openclaw\...`）时会把点目录后的整段误当扩展名删掉，输出写到错误位置（下载 404）；改用 `path.extname` 只取最后路径段的扩展名。
- **下载 404（点目录路径）**：Express send 库默认忽略点开头路径段，解压目录/用户名含点目录时 `res.download` 404；显式 `dotfiles: 'allow'`（file 已限定在 UPLOAD_DIR 内）。

### 端到端验证
- 重新打包后解压→启动→上传→提取→下载全链路通过（含点目录路径、中文文件名、MP3/M4A/WAV）；下载文件头 ID3 校验为有效音频。

## [1.5.0] - 2026-08-29

### 新增
- 自定义输出文件名：输入框留空走默认命名（原视频名 + 新后缀）；多文件批量时自动加序号（名字_1、名字_2…）防同名覆盖；服务端对文件名做安全清洗（防路径穿越/非法字符/超长），不带扩展名自动补目标格式后缀。
- 批量进度可视化：多文件处理时显示「n/总数」进度条与状态（进行中 / 全部完成 / 部分失败 / 已取消）。
- 高级选项面板：比特率（128/192/320 kbps，MP3/Opus）、采样率（44.1/48/96 kHz）、声道（单声道/立体声）；服务端白名单校验，非法值自动忽略。

### 修复
- 修复 v1.4.1 引入的回归：M4A/Opus 源音轨与容器不兼容时的「自动回退重编码」失效，恢复回退逻辑。
- 修复回退路径上 stderr 变量越界引用（ReferenceError）。
- 修复 outputName 未清洗导致的潜在目录穿越写入风险。
- 修复前端未真正提交自定义文件名/高级选项参数、高级选项按钮无事件绑定的问题。

### 端到端验证
- 14 项自动化检查全部通过（中文文件名/回退/穿越防护/ffprobe 参数核验/非法值容错）。

## [1.4.2] - 2026-08-29

### 修复
- 统一错误响应格式：所有失败响应统一携带 `errorCode` 字段。
- `runExtract` 失败时把 `errorCode` / `errorHint` 写入 job 对象，SSE 推送包含结构化错误信息。

### 优化
- SSE 推送增加 `errorCode` / `errorHint` 字段。

## [1.4.1] - 2026-08-29

### 修复
- FFmpeg 失败错误分类：7 种错误码 + 用户提示。
- jobId 长度限制：上限 64 字符。
- stderr 内存优化：按行保留最后 200 行。

### 优化
- SSE 断线重连：指数退避 3s→30s。
- 前端失败状态展示：错误信息填充到状态徽标。

## [1.4.0] - 2026-08-29

### 新增
- 便携版 zip 内文件统一放在 `audio-extractor/` 子目录。
- `build-portable.bat` 优化：自动排除自身、UTF-8 BOM 编码。

## [1.3.3] - 2026-08-27

### 新增
- 新增「安装到桌面.bat」一键创建桌面快捷方式。