# Changelog

本仓库遵循 [语义化版本](https://semver.org/lang/zh-CN/)：`主版本.次版本.补丁`。

## [1.5.0] - 2026-08-29

### 新增
- 自定义输出文件名：输入框留空走默认命名（原视频名 + 新后缀）；多文件批量时自动加序号（名字_1、名字_2…）防同名覆盖；服务端对文件名做安全清洗（防路径穿越/非法字符/超长），不带扩展名自动补目标格式后缀。
- 批量进度可视化：多文件处理时显示「n/总数」进度条与状态（进行中 / 全部完成 / 部分失败 / 已取消）。
- 高级选项面板：比特率（128/192/320 kbps，MP3/Opus）、采样率（44.1/48/96 kHz）、声道（单声道/立体声）；服务端白名单校验，非法值自动忽略。

### 修复
- 修复 v1.4.1 引入的回归：M4A/Opus 源音轨与容器不兼容时的「自动回退重编码」失效，恢复回退逻辑。
- 修复回退路径上 stderr 变量越界引用（ReferenceError）。
- 修复 outputName 未清洗导致的潜在目录穿越写入风险。
- 修复前端未真正提交自定义文件名/高级选项参数、高级选项按钮无事件绑定的问题。

### 端到端验证
- 14 项自动化检查全部通过（中文文件名/回退/穿越防护/ffprobe 参数核验/非法值容错）。

## [1.4.2] - 2026-08-29

### 修复
- 统一错误响应格式：所有失败响应统一携带 `errorCode` 字段。
- `runExtract` 失败时把 `errorCode` / `errorHint` 写入 job 对象，SSE 推送包含结构化错误信息。

### 优化
- SSE 推送增加 `errorCode` / `errorHint` 字段。

## [1.4.1] - 2026-08-29

### 修复
- FFmpeg 失败错误分类：7 种错误码 + 用户提示。
- jobId 长度限制：上限 64 字符。
- stderr 内存优化：按行保留最后 200 行。

### 优化
- SSE 断线重连：指数退避 3s→30s。
- 前端失败状态展示：错误信息填充到状态徽标。

## [1.4.0] - 2026-08-29

### 新增
- 便携版 zip 内文件统一放在 `audio-extractor/` 子目录。
- `build-portable.bat` 优化：自动排除自身、UTF-8 BOM 编码。

## [1.3.3] - 2026-08-27

### 新增
- 新增「安装到桌面.bat」一键创建桌面快捷方式。