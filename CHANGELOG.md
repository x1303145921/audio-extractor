# Changelog

本仓库遵循 [语义化版本](https://semver.org/lang/zh-CN/)：`主版本.次版本.补丁`，新功能递增次版本、缺陷修复递增补丁。

## [1.3.2] - 2026-08-27

### 修复
- **中文文件名乱码（重要）**：服务端（multer/busboy）把 multipart 里的 filename 按 Latin-1 解码，UTF-8 中文文件名被误解码成乱码，导致下载的音频文件名乱码。新增 `fixUtf8Name()` 在两条上传路径（单文件 + 分片）统一还原，下载名 = 原视频名 + 新后缀，中文正常显示。
- **启动脚本解析错乱**：`start.bat` 换行符为 LF（Unix 风格），cmd.exe 解析时把 `cd /d`、`if errorlevel` 拆成碎片报「'e' 不是内部或外部命令」；`启动音频提取工具.bat` 编码为 UTF-8 在中文系统控制台下乱码。两者统一重写为 CRLF + 系统编码，Node 检测改为「同目录 node.exe 优先 → PATH 兜底」（便携包 node 不在 PATH 的问题一并解决）。

### 优化
- 结果卡片新增格式徽标（MP3 / M4A / WAV…），一眼看清输出格式
- 下载按钮点击后短暂反馈「已开始 ✓」
- 文件列表「已完成」状态展示「大小 → 转换后文件名」，过程状态只显示大小（去掉多余分隔符）
- 移除「失败」状态徽标里的 `ⓘ` 残留字符

## [1.3.1] - 2026-08-27

### 修复
- health 接口版本号硬编码为 1.3.0 导致显示错位 → 改为统一从 `package.json` 读取（bump 只需改一处）

### 优化
- 头部 logo 去除渐变底框，直显原初版图标（52px）
- 文件大小支持 GB 单位显示
- 结果汇总条文案中性化（「已生成 N 个文件」）

### 其他
- 补发 v1.1.0 release（版本线叙事完整化），README 新增「版本说明」

## [1.3.0] - 2026-08-27

### 新增
- 桌面级启动体验：最小化静默启动器（`启动工具-最小化.bat`）、页面底部「退出服务」按钮
- 下载名根治：磁盘 hash 名 → 原始文件名（v1.3.2 又修复了其中文编码问题）

### 优化
- UI 打磨、文档更新

## [1.2.0] - 2026-08-26

### 新增
- 零依赖便携版（自带 Node 运行时 + FFmpeg，解压即用）
- PWA 缓存修复

### 优化
- 便携包改用精简版 FFmpeg（体积 -23%）
- 界面打磨

## [1.1.0] - 2026-08-25

正式版系列公开发布的起点（版本线叙事补发，无独立资产；自本版本起 1.x 为正式版）。

## [0.1.1] - 2026-08-24

公开预览期加固版：稳定性与健壮性修复。

## [0.1.0] - 2026-08-23

公开预览期首发：核心链路（上传 → FFmpeg 转码 → 下载）验证版。
