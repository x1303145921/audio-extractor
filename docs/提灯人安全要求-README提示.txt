# 提灯人安全要求（2026-08-30 上传归档 → 已落地 v1.6.3）

## 硬性纪律
- 不要把监听地址改成 0.0.0.0，不要开放局域网访问（本工具为本地离线工具）

## 落地情况（2026-08-30）
### v1.6.3 代码改动（唯一功能改动）
- server.js：BIND 默认值 '0.0.0.0' → '127.0.0.1'（默认仅本机，不再默认开放局域网）
- package.json：1.6.2 → 1.6.3
- README：顶部版本说明 + 里程碑表 + 特性 + 快速开始（NOTE 安全块 + zip 名 + 手机使用步骤）+ 配置项 BIND 表 + FAQ + 文末安全提示 全部同步
- CHANGELOG：[1.6.3] 条目 + 仓库维护说明（旧版本不建议用原因）
- 下载最新版.bat：VER=1.6.3
- 旧版本代码未改动（仅文案说明风险）

### 旧版本（v1.6.2 及更早）不建议使用的原因
- 默认监听 0.0.0.0（开放局域网）且工具无鉴权 → 同一网络内其他设备可访问，存在安全隐患